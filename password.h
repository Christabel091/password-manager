#include<iostream>
#include<string>
#include<vector>
#include "user_password.h"

class User{
    std::string user_name;
    std::vector<eachPassword> passswords;
public:
    User(std::string );
    void password_suggester();
    void change_password();
};